# Dependencies & build output
node_modules/
dist/
*.tsbuildinfo
.pnpm-store/

# Otto runtime (scratch is gitignored; .otto/ content is tracked, state.json is not)
.otto-tmp/
.otto/state.json
.otto/cbm-scratch/
# Eval fixture replay artifacts (otto writes a run bundle inside each fixture)
benchmarks/fixtures/*/.otto/runs/
# docs/ROADMAP.md is temporary planning scratch, not an official roadmap
docs/ROADMAP.md

# Claude Code (shared: settings.json + CLAUDE.md; personal: *.local.json + locks)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# macOS Finder metadata
.DS_Store
.otto/runs/
# Per-composite-identity PR review state + advisory leases (local runtime only)
.otto/review-state/
