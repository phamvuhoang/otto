// Formats a price given in integer cents as a dollar string. Left
// unimplemented so a fan-out sub-agent has real work to land — see
// .otto/tasks/fanout-overlap/tasks.json and this fixture's README.md.
export function formatPrice(cents) {
  throw new Error("not implemented");
}
