// Byte-unit constants. The benchmark feature adds a `humanSize` helper here and
// wires it into report.mjs (a two-file change).
export const KB = 1024;
export const MB = KB * 1024;
export const GB = MB * 1024;
